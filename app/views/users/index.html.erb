<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<div id="users">
  <table class="table table-striped" >
    <thead>
      <tr>
        <th scope="col">First Name</th>
        <th scope="col">Last Name</th>
        <th scope="col">Email</th>
        <th scope="col">Phone</th>
        <th scope="col">Group</th>
        <th scope="col" colspan="3"></th>
      </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
        <td><%= user.phone %></td>
        <td><%= user.group %></td>
        <td><%= link_to "Show", user, class:"btn btn-info"%><td>
        <td><%= link_to "Edit", edit_user_path(user), class:"btn btn-secondary"%><td>
        <td><%= button_to "Delete", @user, method: :delete, class:"btn btn-success" %><td>
      </p>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
<%= link_to "New user", new_user_path, class:"btn btn-secondary"%>
